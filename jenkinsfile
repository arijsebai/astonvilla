pipeline {
    agent any 
    stages {
        
        stage ("Generate docker image") {
            steps {
                 
                sh "docker build -t astonvilla-app:1.1.${env.BUILD_NUMBER} ."
                sh ' docker images'
                               
            }
        }
      
        }
    }

